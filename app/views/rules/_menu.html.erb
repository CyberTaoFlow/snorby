<%= drop_down_for('Change action(s)', 'icons/actions.png', 'incident') do %>
  <% @actions.each do |action| %>
    <%= drop_down_item action.name, '#', nil, { :class => 'actions', :"data-action-id" => action.id.to_i } %>
  <% end %>
<% end %>
