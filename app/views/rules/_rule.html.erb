<li id='rule-<%= rule.id %>' data-rule-id='<%= rule.id %>' class="rule">
  <div class="row">
    <div class="action">Action</div>
    <div class="message click"><%= truncate(rule.msg, :length => 50, :omission => '...') %></div>
  </div>
  <div class="rule-data event-data" style='display:none;'>
    <%= render :partial => 'rules/show', :locals => {:rule => rule} %>
  </div>
</li>